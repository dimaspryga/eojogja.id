export const metadata = {
  title: "Promo | Eojogja.id",
  description: "Special offers and promotions for activities in Jogja",
};

export default function PromoLayout({ children }) {
  return children;
}
