export const metadata = {
  title: "Activities | Eojogja.id",
  description: "Discover exciting activities and experiences in Jogja",
};

export default function ActivityLayout({ children }) {
  return children;
}
